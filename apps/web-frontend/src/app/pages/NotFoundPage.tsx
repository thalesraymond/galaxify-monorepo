import { PageHeader } from '@/shared/ui/PageHeader'

export function NotFoundPage() {
  return (
    <PageHeader
      title="Page not found"
      description="We could not find that page. Check the address or use the primary navigation."
    />
  )
}
